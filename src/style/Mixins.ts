import { css } from '@emotion/react';

export const mixins = {
  backgroundImage: css`
    background: no-repeat center center;
    background-size: cover;
    height: 100vh;
    position: fixed;
    top: 0;
    width: 100vw;
  `,
};
